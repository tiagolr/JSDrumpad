desc:simplex
slider1:freq=440<20, 10000, 1:log>Frequency (Hz)

@init
cycle = 0;
envelope = 1;

@sample

envelope -= 0.000001;
envelope <= 0 ? envelope = 1;

cycle += freq / srate * envelope * 10;
cycle > 1 ? cycle = 0;
wave = sin(cycle * 2 * $pi);

spl0 = spl1 = wave * 0.1;
